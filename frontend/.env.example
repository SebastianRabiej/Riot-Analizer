# Base URL of the backend API.
# In dev the Vite proxy forwards /api -> http://localhost:8080, so the default is fine.
# In prod nginx proxies /api to the backend on the same origin.
VITE_API_BASE_URL=/api
